# 对外调用清单

当前版已迁移到 [`docs/external-api（对外调用清单）.md`](./docs/external-api（对外调用清单）.md)。

保留这个根目录文件是为了兼容旧链接。请以新文档为准，它已经按当前代码重新核对：

- `POST /api/submissions` 使用 wrapper 对象 `{submissionName, items}`。
- 顶层数组、单端 `platform`、旧 `deviceAlias` 已不是当前对外主契约。
- Kafka / Webhook、报告 URL、取消接口、内部 Bearer 接口都在新文档里统一说明。
