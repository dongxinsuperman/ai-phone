# 两地同步：新 Mac 拉下仓库到能跑通

> **本文档定位**：ai-phone 仓库做的是"两台 Mac 之间的进度同步盘"。任意一台 Mac（不管是第一台还是新机）拉下仓库后，按本文档**从上到下做一遍**就能跑起来。
>
> 完整跑通的标志：浏览器进 `http://127.0.0.1:5180`，能看到设备卡片，输入 goal 能跑出 step。

---

## 0. 一眼看完（TL;DR）

```
┌──────────────────────────────────────────────────────────┐
│ 系统级（每台 Mac 装一次）                                │
│   brew + python@3.11 + node + ffmpeg + adb               │
│   Xcode（iOS 用）  +  DevEco Studio（HarmonyOS 用）      │
└──────────────────────────────────────────────────────────┘
                        ↓
┌──────────────────────────────────────────────────────────┐
│ git clone <仓库地址>                                      │
└──────────────────────────────────────────────────────────┘
                        ↓
┌──────────────────────────────────────────────────────────┐
│ 配 .env（必填 5-7 个字段，详见 §3）                       │
│   ↑ 每台 Mac 自己写，永不进 git                           │
└──────────────────────────────────────────────────────────┘
                        ↓
┌──────────────────────────────────────────────────────────┐
│ 装依赖：venv + pip install -e ".[ios,harmony]"            │
│        cd web && npm install                              │
└──────────────────────────────────────────────────────────┘
                        ↓
┌──────────────────────────────────────────────────────────┐
│ iOS 用户：Xcode 登录 Apple ID（一次性）+ 真机 Trust       │
└──────────────────────────────────────────────────────────┘
                        ↓
┌──────────────────────────────────────────────────────────┐
│ 起三个终端：D Server / E Agent / F 前端                  │
└──────────────────────────────────────────────────────────┘
```

---

## 1. 系统级软件准备

### 1.1 必装（所有人）

```bash
# 1) Homebrew
/bin/bash -c "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/HEAD/install.sh)"

# 2) brew 一行装齐基础栈
brew install python@3.11 node ffmpeg android-platform-tools git

# 3) 验证
python3.11 --version    # Python 3.11.x
node -v                  # v18+ 或 v20+
ffmpeg -version | head -1
adb version
```

| 包 | 用途 |
|---|---|
| `python@3.11` | 后端 venv（pyproject.toml 锁了 3.11–3.12） |
| `node` | 前端 vite，自带 npm |
| `ffmpeg` | Android scrcpy 镜像 + iOS wda_mjpeg 镜像必需 |
| `android-platform-tools` | adb（Android 真机） |
| `git` | clone 代码（macOS 自带也行，brew 版更新） |

### 1.2 按需装（看你这台 Mac 接什么真机）

| 接什么 | 还要装什么 |
|---|---|
| **只接 Android** | 上面 §1.1 就够 |
| **接 iOS** | App Store 装 **Xcode**（14GB+，启一次同意 license），然后：`sudo xcode-select -s /Applications/Xcode.app/Contents/Developer` |
| **接 HarmonyOS** | [DevEco Studio](https://developer.huawei.com/consumer/cn/deveco-studio/)（按 Mac 芯片选 ARM 或 X86 版），拖进 `/Applications/`，**不用启动 IDE**，借里面的 hdc 二进制即可 |

iOS 用户额外做：

- 打开 Xcode → Settings → Accounts → 加 Apple ID（免费 ID 也行）输密码

HarmonyOS 用户额外做（把 hdc 加进 PATH）：

```bash
cat >> ~/.zshrc <<'EOF'

# HarmonyOS hdc (DevEco Studio 6.1 路径；旧版本 ≤5.x 路径里没有 default/，自己 find 一下)
export PATH="/Applications/DevEco-Studio.app/Contents/sdk/default/openharmony/toolchains:$PATH"
EOF
source ~/.zshrc

hdc -v                  # 期望 Ver: 3.2.0c 或类似
```

---

## 2. clone 仓库

```bash
# 路径自取。建议英文路径，避免某些 shell 脚本对中文路径过敏
mkdir -p ~/code
cd ~/code
git clone <你的私有仓库地址> ai-phone

# 或者沿用现机路径风格：
# mkdir -p ~/代码文件/sonic合集
# git clone <repo> ~/代码文件/sonic合集/ai-phone
```

> WDA 工程已 **vendored** 在 `third_party/WebDriverAgent/`，不需要单独 clone。

---

## 3. 配置 `backend/.env`（核心步骤）

这是**唯一一个本机自己写的文件**。`.env` 在 `.gitignore` 里，永远不会上传到 GitHub，两台 Mac 各自维护各自的。

### 3.1 复制模板

```bash
cd ai-phone/backend
cp .env.example .env
```

### 3.2 必填字段（5-7 个）

打开 `.env`，按下面表格修改。**其它字段保持模板默认即可**。

| 字段 | 怎么填 |
|---|---|
| `AI_PHONE_DB_URL` | Postgres 连接串。共用远程同事的库 / 本地起 PG 都行（见 §3.3） |
| `AI_PHONE_VLM_API_KEY` | 火山引擎方舟 key（在 [console.volcengine.com/ark](https://console.volcengine.com/ark) 申请，或问另一台 Mac 的 .env 拷过来） |
| `AI_PHONE_ASSISTANT_API_KEY` | 留空则自动复用 `AI_PHONE_VLM_API_KEY`，多数情况不用填 |
| `AI_PHONE_WDA_PROJECT_DIR` | **绝对路径**，写本机 clone 后的位置：`/Users/<本机用户>/<clone 位置>/ai-phone/third_party/WebDriverAgent`（仅 iOS 用户必填） |
| `AI_PHONE_WDA_BUNDLE_ID` | 自定义唯一值，建议 `com.<本机用户名>.wda`（避免免费 Apple ID 同 Bundle Id 撞配额；仅 iOS 用户必填） |
| `AI_PHONE_WDA_TEAM_ID` | 你的 Apple Developer Team ID（10 字符大写，在 [developer.apple.com/account](https://developer.apple.com/account) 查；仅 iOS 用户必填） |
| `AI_PHONE_AGENT_TOKEN` | 默认 `dev` 即可，本地开发不用改 |

### 3.3 关于数据库

两种选项：

| 选项 | .env 怎么写 | 注意 |
|---|---|---|
| **共用远程同事的 PG**（10.8.8.120） | 直接拷另一台 Mac 的 .env DB 串 | **同时只一台 Mac 跑 agent**，否则两边 run id 会撞 |
| **本地各起 PG** | `brew install postgresql@15` → `brew services start postgresql@15` → `createdb auto_app` → 改 `.env` 为 `postgresql+asyncpg://<本机用户>@127.0.0.1:5432/auto_app` | 启动时代码自动建表，全新空库 |

### 3.4 关于 WDA 签名（仅 iOS 用户）

新增的 `AI_PHONE_WDA_BUNDLE_ID` + `AI_PHONE_WDA_TEAM_ID` 会被 agent 在调 `xcodebuild` 时作为命令行 build settings 注入：

```
xcodebuild test ... PRODUCT_BUNDLE_IDENTIFIER=com.<你>.wda DEVELOPMENT_TEAM=<你的Team>
```

效果：`third_party/WebDriverAgent/.../project.pbxproj` 文件**不需要改**（即使 git 上看到的是另一个人的 Bundle Id / Team），每台 Mac 用自己 .env 注入自己的签名，**两机彻底解耦**。

---

## 4. 装依赖

### 4.1 后端 Python venv

```bash
cd ai-phone/backend

# 用 brew 装的 Python 3.11 建 venv（路径按本机芯片）
/opt/homebrew/opt/python@3.11/bin/python3.11 -m venv .venv
#   Intel Mac 改成：/usr/local/opt/python@3.11/bin/python3.11

source .venv/bin/activate
pip install -U pip

# 装主依赖 + 平台 extras（按需选）
pip install -e .                       # 必须
pip install -e ".[ios]"                # iOS 真机要
pip install -e ".[harmony]"            # HarmonyOS 真机要
# 想合并：pip install -e ".[ios,harmony]"
```

### 4.2 前端

```bash
cd ai-phone/web
npm install
# 慢的话切源：npm config set registry https://registry.npmmirror.com
```

---

## 5. iOS 用户额外步骤（一次性）

> 没接 iOS 跳过本节。

### 5.1 Xcode 必须已经登录 Apple ID

打开 Xcode → Settings → Accounts → 你的 Apple ID 输密码。

之后 Xcode 会把这个 Apple ID 的开发者私钥 + 证书写进 macOS Keychain。`xcodebuild test` 自动从 Keychain 读签名信息。

### 5.2 真机首次接入

1. 数据线插 iPhone → 弹"信任此电脑"点信任
2. iPhone → 设置 → 隐私与安全性 → 开发者模式 → 打开（要求重启）
3. 重启后再亮屏 → 设置里再次进开发者模式 → 按提示打开 + 输密码

### 5.3 第一次跑 agent 后，iPhone 上还要再点一次"信任开发者"

第一次 agent 跑 `xcodebuild test`：
- xcodebuild 编译完，把 WDA app 装到 iPhone 上
- iPhone 弹「无法验证 App」（首次安装、新签名都会弹）
- iPhone 设置 → 通用 → VPN 与设备管理 → 找到你的 Apple ID 下"开发者 App"分类 → 点信任
- 重启 agent 或等 45s 自动 retry → iPhone 上出现 `Automation Running` 灰屏 = 成功

> **以后什么时候还会要求重新点信任**：免费 Apple ID profile 7 天到期、iOS 大版本升级、换路径触发 DerivedData 重建。本质都是 Apple 检测到"新签名"，要求重新审批一次。

### 5.4 每次 iPhone 重启后挂 DDI（一行命令）

```bash
cd ai-phone/backend
source .venv/bin/activate
sudo -E .venv/bin/python -m pymobiledevice3 mounter auto-mount --udid <你的UDID>
```

看到 `mounted successfully` 或 `already mounted` 即可。

---

## 6. 起三个终端

详细命令走 [`启动终端清单.md` §9](./启动终端清单.md#9-命令参考)。基础流程：

### 终端 D — Server

```bash
cd ai-phone/backend
source .venv/bin/activate
uvicorn ai_phone.server.app:app --host 0.0.0.0 --port 8000 --reload
```

预期：日志末尾 `Application startup complete.`

### 终端 E — Agent

```bash
cd ai-phone/backend
source .venv/bin/activate
python -m ai_phone agent
```

预期：扫到真机 → 注册到 Server → 前端设备卡出现。

### 终端 F — 前端

```bash
cd ai-phone/web
npm run dev
```

预期：`Local: http://127.0.0.1:5180/`，浏览器打开看到设备卡。

### iOS 用户额外：终端 A — pmd3 tunneld（开机一次，常驻）

```bash
cd ai-phone/backend
sudo .venv/bin/pymobiledevice3 remote tunneld
```

不要 Ctrl+C，agent 截图 / lockdown 查询都依赖它。

---

## 7. 完工自检

跑通就算搬完，打勾打到全绿：

- [ ] `python3.11 -V` `node -v` `ffmpeg -version` `adb version` 全过
- [ ] iOS：`xcodebuild -version` 过 + Xcode 已登录 Apple ID
- [ ] Harmony：`hdc -v` 过
- [ ] `cd ai-phone/backend && source .venv/bin/activate && python -c "import ai_phone"` 不报错
- [ ] `cd ai-phone/web && ls node_modules/vue` 存在
- [ ] D / E / F 三终端全部起来，无红色错误日志
- [ ] iOS：iPhone 屏出现 `Automation Running` 灰屏
- [ ] 浏览器进 `http://127.0.0.1:5180`，看到设备卡
- [ ] 输个简单 goal（例如"打开设置"），step 列表能跑动

---

## 8. 两地协作约定

两台 Mac 都搭好以后日常协作：

| 场景 | 怎么做 |
|---|---|
| **代码同步** | 一边 push、另一边 pull。`.env` / `.venv/` / `node_modules/` / `.data/` 都在 .gitignore 里，自然不同步 |
| **数据库共用** | 两台都连 10.8.8.120。**同时只一台跑 agent**（避免设备序列号 / run id 撞车） |
| **真机切换** | 拔下来插到另一台 Mac：iOS 必须重新弹"信任此电脑" + 重新挂 DDI；Android / Harmony 重新点 USB 调试授权 |
| **VLM key 用量** | 共用同一个 ark key 没问题，方舟控制台看 token 总开销即可 |

---

## 9. 常见问题速查

| 现象 | 原因 | 怎么办 |
|---|---|---|
| `pip install -e .` 报 Python 版本不支持 | 用了系统自带老 Python（3.9） | venv 时显式指定：`/opt/homebrew/opt/python@3.11/bin/python3.11 -m venv .venv` |
| `npm install` 卡半小时 | 默认 npm 源在国外 | `npm config set registry https://registry.npmmirror.com` |
| 后端报 `connect failed: 10.8.8.120:5432` | 网络不通 | 连 VPN，或本机起 PG（§3.3 选项 2） |
| Agent 报 `xcodebuild: error: ... project ... does not exist` | `.env` 里 `AI_PHONE_WDA_PROJECT_DIR` 路径错了 | 改成本机 clone 实际位置 |
| Agent 报 `Developer App Certificate is not trusted` | iPhone 上还没点信任 | iPhone → 设置 → 通用 → VPN 与设备管理 → 信任开发者证书 |
| iOS 镜像黑屏 | DDI 没挂 | §5.4 重新挂 |
| 端口 8000 被占 | 老进程没退干净 | `lsof -i :8000` 看 pid → `kill -9 <pid>` |

---

## 附：本仓库主要目录

```
ai-phone/
├── AGENTS.md                  ← AI 协作入口（路由表）
├── README.md                  ← 项目概览
├── 两地同步.md                 ← 你正在看的这份
├── 启动终端清单.md             ← 日常开终端的命令参考
├── 架构设计.md                 ← 模块 / 数据流
├── HarmonyOS环境配置笔记.md
├── iOS_WDA_Xcode操作手册...
├── ...更多设计文档
├── backend/
│   ├── .env                   ← 本机敏感配置（永不进 git）
│   ├── .env.example           ← 模板（进 git，参考它写自己的 .env）
│   ├── pyproject.toml
│   └── ai_phone/...           ← Python 包：server / agent / shared 三角色共用
├── web/
│   ├── package.json
│   └── src/...
├── third_party/
│   └── WebDriverAgent/        ← vendored，xcodebuild 直接用
└── tools/
    └── build-wda-ipa.sh
```
